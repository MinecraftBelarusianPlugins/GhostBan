package by.siarhiejbahdaniec.ghostban.config

object ConfigKeys {
    const val ghostSpawnX = "ghost-spawn-point.x"
    const val ghostSpawnY = "ghost-spawn-point.y"
    const val ghostSpawnZ = "ghost-spawn-point.z"
    const val ghostSpawnWorld = "ghost-spawn-point.world"

    const val ghostMessage = "ghost-message"
    const val humanityMessage = "humanity-message"
}